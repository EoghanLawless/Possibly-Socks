include ../layout
    div.row
        div.col-md-4.col-md-offset-4
            h1 Sign up
            form(action="", method="post")
                div.form-group
                    label(for="email") E-mail
                    imput.form-control(type="text", id="email", name="email")
                div.form-group
                    label(for="password") Password
                    imput.form-control(type="password", id="password", name="password")
                input(type="hidden", name="_crsf", value=token)
                #{token}